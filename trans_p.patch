diff --git a/iob/CMakeLists.txt b/iob/CMakeLists.txt
index 92bf07e..8937959 100644
--- a/iob/CMakeLists.txt
+++ b/iob/CMakeLists.txt
@@ -1,4 +1,4 @@
-add_definitions(-DHRPSYS_PACKAGE_VERSION=\"\\"${CPACK_PACKAGE_VERSION_MAJOR}.${CPACK_PACKAGE_VERSION_MINOR}.${CPACK_PACKAGE_VERSION_PATCH}\\"\" -DROBOT_IOB_VERSION=4)
+add_definitions(-Wno-deprecated -DHRPSYS_PACKAGE_VERSION=\"\\"${CPACK_PACKAGE_VERSION_MAJOR}.${CPACK_PACKAGE_VERSION_MINOR}.${CPACK_PACKAGE_VERSION_PATCH}\\"\" -DROBOT_IOB_VERSION=4)
 
 # https://github.com/start-jsk/rtmros_choreonoid/issues/296
 # catkin bt --cmake-args -DCMAKE_BUILD_TYPE={Release, Debug}
@@ -7,19 +7,19 @@ if(NOT CMAKE_BUILD_TYPE)
 endif()
 
 # hrpIo
-add_library(hrpIo_choreonoid SHARED iob.cpp)
+add_library(hrpIo_choreonoid_wheel SHARED iob.cpp)
 add_library(hrpIo_shm SHARED iob-shm.cpp)
 #add_dependencies(hrpIo_choreonoid hrpsys_choreonoid_msgs_gencpp)
 ## include_directories(${ROBOTHARDWARE_SOURCE})
 include_directories(${PROJECT_SOURCE_DIR}/iob)
 
-target_link_libraries(hrpIo_choreonoid rt roscpp rostime)
+target_link_libraries(hrpIo_choreonoid_wheel rt roscpp rostime)
 target_link_libraries(hrpIo_shm rt)
 
 # RobotHardware choreonoid
 message ("RobotHardware source directory: ${ROBOTHARDWARE_SOURCE}")
 set(comp_source robot.cpp RobotHardware_choreonoid.cpp RobotHardware.cpp RobotHardwareService_impl.cpp)
-set(libs hrpIo_choreonoid)
+set(libs hrpIo_choreonoid_wheel)
 foreach(hrplib hrpModel-3.1 hrpCollision-3.1 hrpUtil-3.1)
   find_library(libpath NAMES ${hrplib} PATHS ${openhrp3_LIBRARY_DIRS} NO_DEFAULT_PATH)
   list(APPEND libs ${libpath})
@@ -54,7 +54,7 @@ set_target_properties(hrpEC_trans PROPERTIES OUTPUT_NAME hrpEC)
 set_target_properties(hrpEC_trans PROPERTIES COMPILE_DEFINITIONS "OPENRTM_VERSION110")
 
 ##
-install(TARGETS hrpIo_choreonoid
+install(TARGETS hrpIo_choreonoid_wheel
   LIBRARY DESTINATION lib CONFIGURATIONS Release Debug RelWithDebInfo
 )
 install(TARGETS RobotHardware_choreonoid
